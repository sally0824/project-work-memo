<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>项目工作备忘录 · V2</title>
<style>
  :root{
    --bg:#f5f7fa; --panel:#ffffff; --line:#e5e9ef; --line-strong:#d8dee8;
    --text:#162033; --muted:#667085; --soft:#f8fafc; --primary:#2457a7; --primary-2:#eaf1fb;
    --warning:#b7791f; --warning-bg:#fff7e6; --danger:#b42318; --danger-bg:#fff1f0;
    --success:#227a55; --success-bg:#eef8f3; --shadow:0 8px 28px rgba(16,24,40,.06);
    --radius:12px;
  }
  *{box-sizing:border-box}
  html,body{margin:0;min-height:100%;font-family:"Microsoft YaHei","PingFang SC","Noto Sans CJK SC",Arial,sans-serif;color:var(--text);background:var(--bg)}
  button,input,select,textarea{font:inherit}
  button{cursor:pointer}
  .app{display:grid;grid-template-columns:226px 1fr;min-height:100vh}
  aside{background:#fff;border-right:1px solid var(--line);padding:22px 14px;position:sticky;top:0;height:100vh;overflow:auto}
  .brand{padding:4px 10px 20px;border-bottom:1px solid var(--line);margin-bottom:12px}
  .brand h1{font-size:18px;margin:0 0 7px;letter-spacing:.2px}
  .brand p{font-size:12px;line-height:1.55;color:var(--muted);margin:0}
  .nav-btn{width:100%;border:0;background:transparent;color:#44506a;text-align:left;padding:10px 11px;border-radius:8px;margin:2px 0;display:flex;align-items:center;gap:9px;font-size:14px}
  .nav-btn:hover{background:#f5f7fb}
  .nav-btn.active{background:var(--primary-2);color:var(--primary);font-weight:700}
  .nav-dot{width:7px;height:7px;border-radius:50%;background:#a9b2c2;flex:0 0 auto}
  .nav-btn.active .nav-dot{background:var(--primary)}
  .side-bottom{margin-top:18px;border-top:1px solid var(--line);padding-top:14px}
  .side-bottom button{width:100%;margin-bottom:8px}
  main{min-width:0}
  .topbar{height:68px;background:rgba(255,255,255,.94);border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;padding:0 28px;position:sticky;top:0;z-index:20;backdrop-filter:blur(10px)}
  .project-title{min-width:0}
  .project-title strong{display:block;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:760px}
  .project-title span{display:block;color:var(--muted);font-size:12px;margin-top:3px}
  .top-actions{display:flex;align-items:center;gap:8px}
  .content{padding:24px 28px 46px;max-width:1600px;margin:0 auto}
  .page{display:none}.page.active{display:block}
  .page-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:18px}
  .page-head h2{font-size:22px;margin:0 0 6px}
  .page-head p{margin:0;color:var(--muted);font-size:13px;line-height:1.6}
  .btn{border:1px solid var(--line-strong);background:#fff;color:#344054;padding:8px 12px;border-radius:8px;font-size:13px;min-height:36px}
  .btn:hover{background:#f8fafc}
  .btn.primary{background:var(--primary);border-color:var(--primary);color:#fff}
  .btn.primary:hover{background:#1f4d95}
  .btn.soft{background:var(--primary-2);border-color:#d7e4f7;color:var(--primary)}
  .btn.danger{color:var(--danger);border-color:#f0c7c4;background:#fff}
  .btn.small{padding:5px 8px;min-height:29px;font-size:12px}
  .grid{display:grid;gap:14px}.grid.kpi{grid-template-columns:repeat(4,minmax(0,1fr))}.grid.two{grid-template-columns:1.12fr .88fr}
  .card{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow)}
  .card-pad{padding:18px}
  .kpi-card{padding:16px 18px}
  .kpi-label{font-size:12px;color:var(--muted);margin-bottom:7px}
  .kpi-value{font-size:26px;font-weight:800;letter-spacing:-.5px}
  .kpi-sub{font-size:12px;color:var(--muted);margin-top:6px}
  .progress{height:8px;border-radius:999px;background:#edf1f5;overflow:hidden}.progress>i{display:block;height:100%;background:var(--primary);width:0}
  .section-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:13px}
  .section-title h3{font-size:16px;margin:0}.section-title p{font-size:12px;color:var(--muted);margin:0}
  .quick-form{padding:18px}
  .form-grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:13px}
  .field{grid-column:span 3}.field.span2{grid-column:span 2}.field.span4{grid-column:span 4}.field.span6{grid-column:span 6}.field.span8{grid-column:span 8}.field.span12{grid-column:1/-1}
  label{display:block;font-size:12px;color:#475467;font-weight:700;margin:0 0 6px}
  .req::after{content:" *";color:var(--danger)}
  input,select,textarea{width:100%;border:1px solid var(--line-strong);border-radius:8px;background:#fff;color:var(--text);padding:9px 10px;outline:none;transition:.15s}
  input,select{height:38px} textarea{min-height:80px;resize:vertical;line-height:1.55}
  input:focus,select:focus,textarea:focus{border-color:#8fb0df;box-shadow:0 0 0 3px rgba(36,87,167,.08)}
  .hint{font-size:11px;color:#8a94a6;margin-top:5px;line-height:1.45}
  details.advanced{border-top:1px dashed var(--line-strong);margin-top:14px;padding-top:12px}
  details.advanced summary{font-size:13px;color:var(--primary);font-weight:700;cursor:pointer;user-select:none;margin-bottom:12px}
  .form-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:14px}
  .table-card{overflow:hidden}
  .toolbar{padding:12px 14px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;gap:10px;background:#fbfcfe}
  .toolbar-left,.toolbar-right{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
  .search{width:230px}
  .table-wrap{overflow:auto;max-height:560px}
  table{width:100%;border-collapse:separate;border-spacing:0;font-size:13px;min-width:920px}
  th{position:sticky;top:0;z-index:2;background:#f8fafc;color:#475467;font-size:12px;text-align:left;padding:10px 11px;border-bottom:1px solid var(--line);white-space:nowrap}
  td{padding:10px 11px;border-bottom:1px solid #edf0f4;vertical-align:top;line-height:1.5;color:#344054}
  tr:hover td{background:#fcfdff}
  td.empty{text-align:center;color:#98a2b3;padding:34px}
  .status{display:inline-flex;align-items:center;border-radius:999px;padding:3px 8px;font-size:11px;font-weight:700;white-space:nowrap}
  .status.todo{background:#f2f4f7;color:#667085}.status.doing{background:#eef4ff;color:#2457a7}.status.done{background:var(--success-bg);color:var(--success)}.status.hold{background:var(--warning-bg);color:var(--warning)}
  .mini-actions{display:flex;gap:5px;white-space:nowrap}
  .muted{color:var(--muted)}
  .truncate{max-width:330px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .tag{display:inline-block;border:1px solid #dce4ef;background:#f8fafc;color:#52627b;border-radius:6px;padding:2px 6px;font-size:11px;margin:1px 3px 1px 0}
  .notice{border:1px solid #d9e6f8;background:#f5f9ff;border-radius:10px;padding:12px 14px;color:#3f5f8f;font-size:12px;line-height:1.65;margin-bottom:14px}
  .warning{border-color:#f0d6a8;background:#fffaf0;color:#875e1c}
  .empty-state{padding:30px;text-align:center;color:var(--muted)}
  .empty-state strong{display:block;color:#475467;margin-bottom:5px}
  .gantt-wrap{overflow:auto;padding:8px 0 12px}
  .gantt{min-width:1080px}
  .gantt-head,.gantt-row{display:grid;grid-template-columns:300px 1fr;align-items:stretch}
  .gantt-label{padding:9px 12px;border-bottom:1px solid var(--line);font-size:12px;min-width:0;background:#fff}
  .gantt-label strong{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.gantt-label span{display:block;color:var(--muted);margin-top:3px}
  .gantt-label .kind{display:inline-block;font-size:10px;font-weight:700;color:#667085;background:#f2f4f7;border-radius:5px;padding:1px 5px;margin-right:5px}
  .gantt-section{display:grid;grid-template-columns:300px 1fr;background:#f8fafc;border-bottom:1px solid var(--line)}
  .gantt-section div{padding:7px 12px;font-size:11px;font-weight:800;color:#667085;letter-spacing:.2px}.gantt-section div:last-child{border-left:1px solid var(--line)}
  .timeline{position:relative;border-bottom:1px solid var(--line);min-height:48px;background-image:linear-gradient(to right,#eef1f5 1px,transparent 1px);background-size:calc(100% / var(--days)) 100%}
  .timeline.head{min-height:38px;background:#f8fafc}
  .day-label{position:absolute;top:0;height:38px;border-left:1px solid #e7ebf0;padding:10px 5px;font-size:10px;color:#667085;white-space:nowrap;overflow:hidden}
  .bar{position:absolute;height:10px;border-radius:999px;left:0;z-index:2}
  .bar.project{top:18px;height:12px;background:#8095b6}
  .bar.phase{top:19px;background:#82a6d7}
  .bar.plan{top:11px;background:#c5d2e6}
  .bar.progress-fill{top:11px;background:var(--primary);z-index:3}
  .bar.actual-mark{width:8px!important;height:8px;transform:rotate(45deg);border-radius:1px;background:#2457a7;top:27px;z-index:4}
  .bar.deliverable-mark{width:9px!important;height:9px;transform:rotate(45deg);border-radius:1px;background:#d6992f;top:20px;z-index:4}
  .today-line{position:absolute;top:0;bottom:0;width:1px;background:#b42318;opacity:.55;z-index:5;pointer-events:none}
  .legend{display:flex;gap:14px;align-items:center;font-size:11px;color:var(--muted);flex-wrap:wrap}
  .legend i{display:inline-block;width:18px;height:6px;border-radius:99px;margin-right:5px;vertical-align:1px}.legend .project{background:#8095b6}.legend .phase{background:#82a6d7}.legend .p{background:#c5d2e6}.legend .a{background:var(--primary)}.legend .m{width:7px;height:7px;border-radius:1px;transform:rotate(45deg);background:#d6992f}
  .modal-mask{position:fixed;inset:0;background:rgba(15,23,42,.4);display:none;align-items:center;justify-content:center;padding:20px;z-index:99}
  .modal-mask.show{display:flex}.modal{width:min(820px,100%);max-height:90vh;overflow:hidden;background:#fff;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.18);display:flex;flex-direction:column}
  .modal-head{padding:16px 18px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;flex:0 0 auto;background:#fff;z-index:2}.modal-head h3{margin:0;font-size:17px}
  .modal-body{padding:18px;overflow:auto;min-height:0;flex:1 1 auto}.modal-foot{padding:14px 18px;border-top:1px solid var(--line);display:flex;justify-content:flex-end;gap:8px;flex:0 0 auto;background:#fff}
  .inline-head{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:6px}.inline-head label{margin:0}
  .inline-link{border:0;background:transparent;color:var(--primary);font-size:11px;padding:0;cursor:pointer}
  .subhead{grid-column:1/-1;font-size:12px;font-weight:800;color:#344054;padding-top:5px;border-top:1px dashed var(--line);margin-top:2px}
  .icon-close{border:0;background:transparent;font-size:24px;color:#667085;line-height:1;padding:2px 6px}
  .settings-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}
  .settings-grid .card{padding:18px}.settings-grid h3{margin:0 0 12px;font-size:16px}
  .backup-actions{display:flex;gap:8px;flex-wrap:wrap}
  .toast{position:fixed;right:24px;bottom:24px;background:#15223a;color:#fff;border-radius:9px;padding:10px 14px;font-size:12px;box-shadow:0 10px 30px rgba(0,0,0,.2);opacity:0;transform:translateY(10px);pointer-events:none;transition:.2s;z-index:120}
  .toast.show{opacity:1;transform:translateY(0)}
  .mobile-toggle{display:none}
  @media(max-width:1050px){.grid.kpi{grid-template-columns:repeat(2,1fr)}.grid.two{grid-template-columns:1fr}.field,.field.span2,.field.span4{grid-column:span 6}.field.span6,.field.span8{grid-column:1/-1}}
  @media(max-width:780px){.app{grid-template-columns:1fr}aside{position:fixed;left:-242px;z-index:80;width:226px;transition:.2s;box-shadow:var(--shadow)}aside.open{left:0}.mobile-toggle{display:inline-flex}.topbar{padding:0 14px}.content{padding:18px 14px 40px}.grid.kpi{grid-template-columns:1fr 1fr}.field,.field.span2,.field.span4,.field.span6,.field.span8{grid-column:1/-1}.page-head{align-items:flex-start;flex-direction:column}.top-actions .hide-mobile{display:none}.settings-grid{grid-template-columns: